import type { Tensor } from "./Tensor.js"
import type { Shape } from "./shape.js"

export function randomTensor(
  centralValue: number,
  variance: number,
  shape: Shape,
): Tensor {
  throw new Error()
}
